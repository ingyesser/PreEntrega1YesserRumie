import React from "react";
import { Container } from "react-bootstrap";

const ItemListContainer = ({ greeting }) => {
  return <h2 className="bienvenido">{greeting}</h2>;
};

export default ItemListContainer;
